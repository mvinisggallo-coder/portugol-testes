programa {
  funcao real calcularMedia(real a, real b, real c, real d ){
    retorne (a + b + c + d)/4
  }
  funcao inicio() {
    real media
    media = calcularMedia(10,2,4,4)
    escreva("Média = ", media)
  }
}
